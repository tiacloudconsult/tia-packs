---
    name: portGen
    description: "updating kong-deployment-tiacourse.yaml"
    enabled: true
    runner_type: python-script
    entry_point: portGen.py

    parameters:
      git_branch:
        type: "string"
        description: "The path to the clone directory"
        default: ""
        required: true
      port_var:
        type: "string"
        description: "port number for kong-proxy"
        default: ""
        required: true